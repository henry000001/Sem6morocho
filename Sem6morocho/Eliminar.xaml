<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sem6morocho.Eliminar">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Ingrese el codigo para eliminar" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
            <Entry x:Name="txtCodigo" Placeholder="Ingrese el codigo" Keyboard="Numeric"/>
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>


                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition>

                    </ColumnDefinition>
                    <ColumnDefinition>

                    </ColumnDefinition>

                </Grid.ColumnDefinitions>


                <Button x:Name="btnEliminar" Text="Eliminar" Clicked="btnEliminar_Clicked" Grid.Row="0" Grid.Column="0" BackgroundColor="#2196F3" TextColor="White"/>
                <Button x:Name="btnRegresar" Text="Regresar" Clicked="btnRegresar_Clicked" Grid.Row="0" Grid.Column="1" BackgroundColor="#2196F3" TextColor="White"/>


            </Grid>
        
        </StackLayout>
    </ContentPage.Content>
</ContentPage>